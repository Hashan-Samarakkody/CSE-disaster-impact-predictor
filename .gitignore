__pycache__/
*.py[cod]
.pytest_cache/
*settings.json
# Regenerable artifact cache (stage 01 rebuilds it). Figures in docs/figures ARE tracked,
# so the thesis can cite them by a stable path.
disaster_finance_predictor/artifacts/
