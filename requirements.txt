# Core numerics and data handling
numpy>=1.24
pandas>=2.0
pyarrow>=14.0          # parquet artifact cache

# Excel readers for the CSE archive (.xls) and the EM-DAT export (.xlsx)
xlrd>=2.0.1
openpyxl>=3.1

# Models
scikit-learn>=1.3
xgboost>=2.0
torch>=2.2             # shallow multi-task MLP, trained in a subprocess
statsmodels>=0.14      # VIF, ADF

# Live external sources (stage 01)
yfinance>=0.2
wbgapi>=1.0.14
# NASA POWER, DesInventar, FRED and Wikidata are fetched with stdlib urllib.

# Explainability, figures, notebooks, tests
shap>=0.45
matplotlib>=3.8
jupyter>=1.0
streamlit>=1.35        # demo web app (app.py)
pytest>=8.0
